"""
轻度润色 - 修正语音识别错误，保持原意
"""

# ==================== 基本信息 ====================
name = '轻度润色'                    # 角色名称
match = True                        # 是否启用前缀匹配
process = True                      # 是否启用 LLM 处理

# ==================== API 配置 ====================
# 从 config.json 读取的配置将自动覆盖这里的默认值
provider = 'openai'                     # API 提供商
api_url = 'http://127.0.0.1:8045/v1/'   # API URL（您的代理地址）
api_key = 'sk-fb6e70d98e8d4057a9590885c15e4779'  # API Key
model = 'gpt-4o'                        # 模型名称

# ==================== 上下文管理 ====================
max_context_length = 4096               # 最大上下文长度

# ==================== 功能配置 ====================
enable_hotwords = True                  # 是否启用热词
enable_rectify = True                   # 是否读取潜在纠错记录
enable_thinking = False                 # 是否启用思考
enable_history = False                  # 不保留对话历史（轻度润色无需历史）
enable_read_selection = False           # 是否启用获取选中文字

# ==================== 输出配置 ====================
output_mode = 'typing'                  # 输出方式：直接打字

# ==================== 生成参数 ====================
temperature = 0.3                       # 低温度，更稳定
top_p = 0.9
max_tokens = 2048
stop = ''

# ==================== 提示词 ====================
prompt_prefix_hotwords = '热词列表：'
prompt_prefix_rectify = '潜在纠错记录：'

# ==================== 系统提示词 ====================
system_prompt = """你是一个语音识别文本润色助手。你的任务是修正语音识别可能产生的错误，同时保持原意不变。

规则：
1. 修正明显的同音字错误
2. 修正标点符号
3. 不要改变原文的意思和风格
4. 不要添加额外的内容
5. 保持简洁，直接输出修正后的文本
6. 如果原文没有问题，直接输出原文

只输出修正后的文本，不要有任何解释。"""
