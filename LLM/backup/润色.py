"""
润色 - 修正语音识别错误，保持原意
"""

# ==================== 基本信息 ====================
name = '润色'                         # 角色名称
match = True                        # 是否启用前缀匹配
process = True                      # 是否启用 LLM 处理

# ==================== API 配置 ====================
# 从 config.json 读取的配置将自动覆盖这里的默认值
provider = 'openai'                     # API 提供商
api_url = ''   # API URL（您的代理地址）
api_key = ''  # API Key
model = 'gpt-4o'                        # 模型名称

# ==================== 上下文管理 ====================
max_context_length = 4096               # 最大上下文长度

# ==================== 功能配置 ====================
enable_hotwords = True                  # 是否启用热词
enable_rectify = True                   # 是否读取潜在纠错记录
enable_thinking = False                 # 是否启用思考
enable_history = False                  # 不保留对话历史（轻度润色无需历史）
enable_read_selection = False           # 是否启用获取选中文字

# ==================== 输出配置 ====================
output_mode = 'typing'                  # 输出方式：直接打字

# ==================== 生成参数 ====================
temperature = 0.3                       # 低温度，更稳定
top_p = 0.9
max_tokens = 2048
stop = ''

# ==================== 提示词 ====================
prompt_prefix_hotwords = '热词列表：'
prompt_prefix_rectify = '潜在纠错记录：'

# ==================== 系统提示词 ====================
system_prompt = """你是一个智能语音助手，负责对语音识别后的文本进行润色、修正和格式化。

核心目标：
1. **修正语音错误**：识别并修复同音字错误、标点符号错误。
2. **处理口语改口**：当检测到用户的改口、重复或自我修正（如“不对”、“是”、“我想说的是”）时，自动剔除错误内容，只保留最终确认的正确表述。
3. **结构化整理**：
   - 敏锐捕捉列表逻辑（如“第一”、“首先”、“1是”）。
   - 一旦发现此类逻辑，自动将其格式化为清晰的分行列表（Markdown 格式）。
   - 如果上下文是连续的列表项（如用户先说了“第一点...”，现在补充“第二点...”），请自动保持格式一致性，换行输出。
4. **保持原意**：在修正和整理的同时，严禁通过删减或增加内容来改变用户的原意。

输出要求：
- 直接输出最终的、干净的、格式化好的文本。
- 不要包含任何“好的”、“修正如下”等废话。"""
